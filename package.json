{
	"scripts": {
		"build": "tsup src/main.ts --format iife --clean --minify",
		"build_debug": "tsup src/main.ts --format iife"
	},
	"devDependencies": {
		"tsup": "^8.5.1"
	},
	"license": "LGPL-3.0-or-later"
}